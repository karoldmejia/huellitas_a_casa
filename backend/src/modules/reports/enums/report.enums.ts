export enum ReportType {
    LOST = 'LOST',
    FOUND = 'FOUND',
}

export enum ReportStatus {
    ACTIVE = 'ACTIVE',
    RESOLVED = 'RESOLVED',
    HIDDEN = 'HIDDEN',
    DELETED = 'DELETED',
}

export enum Species {
    DOG = 'DOG',
    CAT = 'CAT',
    OTHER = 'OTHER',
}

export enum Gender {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    UNKNOWN = 'UNKNOWN',
}

export enum Size {
    SMALL = 'SMALL',
    MEDIUM = 'MEDIUM',
    LARGE = 'LARGE',
}